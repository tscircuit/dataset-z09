{
  "name": "dataset-z09-cf-worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --project tsconfig.json --noEmit",
    "seed": "bun run scripts/seed-deployment-cache.ts",
    "profile": "bun run scripts/profile-deployment.ts",
    "profile:batch": "bun run scripts/profile-batch-deployment.ts"
  },
  "devDependencies": {
    "wrangler": "^4.40.0"
  }
}
